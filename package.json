{
  "name": "rift-flame-unified",
  "version": "1.0.0",
  "description": "Unified entry point for Rift Flame (SafeReport) Full-Stack App",
  "main": "server/server.js",
  "scripts": {
    "build:frontend": "cd safereport && npm install && npm run build",
    "build:backend": "cd server && npm install",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "node server/server.js",
    "dev": "concurrently \"cd safereport && npm run dev\" \"cd server && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
